# How to deploy

## 1. Create project directory
mkdir mc-mod-checker && cd mc-mod-checker

## 2. Save all files (app.py, docker-compose.yml, Dockerfile, requirements.txt, and index.html from above)

## 3. Build and run
docker-compose up --build

## 4. Access at http://localhost:8000
